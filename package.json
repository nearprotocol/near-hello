{
    "name": "near-hello",
    "version": "0.1.0",
    "dependencies": {
        "assemblyscript-json": "^0.2.0",
        "near-runtime-ts": "^0.3.1"
    },
    "scripts": {
        "build": "mkdir -p ./out/assembly && gulp"
    },
    "devDependencies": {
        "bignum": "github:MaxGraey/bignum.wasm",
        "gulp": "^4.0.2",
        "near-shell": "^0.11.0"
    }
}
