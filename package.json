{
    "name": "near-hello",
    "version": "0.4.0-preview",
    "dependencies": {
        "assemblyscript-json": "^0.2.0",
        "near-runtime-ts": "^0.3.1"
    },
    "files": [
        "dist/main.wasm"
    ],
    "scripts": {
        "build": "mkdir -p ./dist/assembly && gulp",
        "prepublish": "not-in-install && yarn build || in-install"
    },
    "devDependencies": {
        "bignum": "github:MaxGraey/bignum.wasm",
        "gulp": "^4.0.2",
        "in-publish": "^2.0.0",
        "near-shell": "^0.11.0"
    }
}
